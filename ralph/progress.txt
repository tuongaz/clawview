# Ralph Progress Log
Started: Sat 28 Mar 2026 19:49:06 AEDT

## Codebase Patterns
- Run tests with `uv run pytest tests/` (not bare `uv run pytest`) to exclude sniffly reference tests
- Pricing stored as USD per million tokens in `MODEL_PRICING` dict; division by 1_000_000 happens at calculation time
- Cache cost multipliers: creation = input × 1.25, read = input × 0.10
- Fallback to claude-3-5-sonnet-20241022 pricing for unknown models
- Test classes grouped by concern (e.g., `TestGetModelPricing`, `TestCalculateCost`, `TestCacheCosts`, `TestFallback`)
- JSONL messages have `type` field ("user", "assistant", "agent-name", etc.) and nested `message` dict with `content`, `model`, `usage`, `stop_reason`
- Token usage fields: `input_tokens`, `output_tokens`, `cache_creation_input_tokens`, `cache_read_input_tokens`
- Tool errors detected via `is_error: true` in `tool_result` parts of user message content
- Error categorization uses regex patterns from sniffly's `ERROR_PATTERNS` dict (14 categories)
- `_is_real_user_prompt()` filters out tool-result-only user messages and system boilerplate
- Project dir names encode paths: "-Users-tuongaz-dev-foo" → `/Users/tuongaz/dev/foo`
- `compute_project_stats()` in insights.py accepts encoded project dir name, not full path
- "Command" = real user prompt + all subsequent assistant messages until next real user prompt
- Interruptions detected via `[Request interrupted` in message text or `API Error: Request was aborted.` in content
- Cache cost saved formula: `cache_read * 1.0 - (cache_read * 0.10 + cache_created * 1.25)` (normalized to input token cost)
- `command_details` is computed first, then `user_interactions` is derived from it
- Test fixtures for interruptions must use tool_result content (not plain string) to avoid `_is_real_user_prompt()` treating them as commands
- Use `patch.object(os.path, "expanduser", return_value=str(tmp_path))` to test `compute_project_stats()` with fixture data
- Session → project dir: `find_session_file(sid)` → `os.path.basename(os.path.dirname(fpath))` gives encoded project dir name
- `compute_project_stats()` returns plain dicts, serialize with `json.dumps()` (not Pydantic)
- WS endpoints reuse `_load_session_detail()` for activity-based tick speed

---

## 2026-03-28 - US-001
- Created `src/clawhawk/pricing.py` with hardcoded pricing for 6 Claude models
- Created `tests/test_pricing.py` with 14 unit tests covering all models, cache costs, fallback, and edge cases
- Files changed: `src/clawhawk/pricing.py`, `tests/test_pricing.py`
- **Learnings for future iterations:**
  - sniffly reference uses per-token pricing (division upfront), but ClawHawk uses per-million (cleaner to read, divide at calc time)
  - `uv run pytest` without path picks up sniffly tests which fail due to missing deps — always use `tests/` path
  - pyright runs clean with `from __future__ import annotations` for forward refs
---

## 2026-03-28 - US-002
- Created `src/clawhawk/insights.py` with `compute_project_stats()` function
- Computes 5 stat sections: overview, tools, sessions, daily_stats, hourly_pattern
- Files changed: `src/clawhawk/insights.py`, `ralph/prd.json`
- **Learnings for future iterations:**
  - Sniffly uses a class-based approach (`StatisticsGenerator`) with pre-computed `running_stats`, but ClawHawk uses a simpler functional approach since we recompute on each WS tick
  - Error attribution: errors are on user messages (tool_result with is_error), attributed to the last tool in the preceding assistant message
  - Daily cost is computed by tracking tokens per model per day, then calling `calculate_cost()` from pricing.py
  - Hourly pattern ensures all 24 hours are represented even if no messages exist for some hours
  - `_parse_all_messages()` returns a flat sorted list — this allows all stat functions to iterate once
---

## 2026-03-28 - US-003
- Added 5 new stat sections to `insights.py`: errors, models, user_interactions, cache, command_details
- Error stats: total, rate, by_category (14 categories), details list
- Model stats: per-model token counts (input/output/cache_creation/cache_read) and usage frequency
- User interaction stats: real commands, tool use rate, avg tools/steps/tokens per command, interruption rate, tool_count_distribution, model_distribution
- Cache stats: total created/read, hit rate, efficiency, tokens saved, cost saved, break-even, ROI
- command_details: per-command array with user_message, timestamp, model, steps, tools_count, tokens, interrupted, tool_names
- Updated `_empty_stats()` with all new section defaults
- Files changed: `src/clawhawk/insights.py`, `ralph/prd.json`, `ralph/progress.txt`
- **Learnings for future iterations:**
  - command_details is computed as an intermediate result that user_interactions then derives stats from — avoids double-iteration
  - Interruption marking requires a second pass through messages since interruption signals appear as non-real-prompt user messages after the command
  - Cache efficiency is capped at 100% with `min(100.0, ...)` to handle edge cases
  - Error text in details is truncated to 200 chars to avoid huge payloads
---

## 2026-03-28 - US-004
- Created `tests/test_insights.py` with 44 unit tests covering all stat sections
- JSONL fixture helpers: `_assistant_line`, `_user_prompt_line`, `_tool_result_line`, `_interruption_line`
- Shared pytest fixture creates 2 sessions across 2 days with tools, errors, cache tokens, interruptions, multiple models
- Test classes: TestOverviewStats (5), TestToolStats (3), TestDailyStats (5), TestHourlyPattern (3), TestErrorStats (4), TestModelStats (3), TestUserInteractionStats (6), TestCacheStats (7), TestCommandDetails (4), TestEmptyStats (2), TestTimezoneOffset (2)
- Files changed: `tests/test_insights.py`, `ralph/prd.json`, `ralph/progress.txt`
- **Learnings for future iterations:**
  - Interruption lines in JSONL must be tool_result content (not plain string) to avoid being treated as real user prompts by `_is_real_user_prompt()`
  - Use `patch.object(os.path, "expanduser", ...)` to redirect `compute_project_stats()` to tmp_path fixtures
  - Tool error attribution goes to the LAST tool in the preceding assistant message — important when assistant calls multiple tools
  - `compute_project_stats()` sorts all messages globally by timestamp, so multi-session fixtures interleave chronologically
---

## 2026-03-28 - US-005
- Added `/ws/insights/{sessionId}` WebSocket endpoint in `src/clawhawk/ws.py`
- Resolves session ID → encoded project directory name, then calls `compute_project_stats()`
- Uses file mtime change detection to avoid unnecessary recomputation
- Follows existing adaptive polling pattern (fast tick when session active, slow tick when idle)
- Sends initial payload on connect, pushes updates only when data changes
- Graceful disconnection handling (WebSocketDisconnect)
- Registered route in `src/clawhawk/app.py`
- Files changed: `src/clawhawk/ws.py`, `src/clawhawk/app.py`, `ralph/prd.json`, `ralph/progress.txt`
- **Learnings for future iterations:**
  - Session → project resolution: `find_session_file()` returns JSONL path, `os.path.basename(os.path.dirname(fpath))` gives encoded project dir name
  - Mtime-based change detection: glob JSONL files, track max mtime, skip recomputation when unchanged
  - `compute_project_stats()` returns plain dicts (not Pydantic models), so use `json.dumps()` not `model_dump_json()`
  - The `_load_session_detail()` helper is reused across WS endpoints for activity checks
  - json import was moved to top-level in ws.py (was previously local in `_serialize_memory_files`)
---

## 2026-03-28 - US-006
- Added `ProjectInsights` TypeScript interface and all sub-interfaces to `frontend/src/types.ts`
- Sub-interfaces: InsightsOverview, InsightsTools, InsightsSessions, DailyStatEntry, HourlyTokens, InsightsHourlyPattern, ErrorDetail, InsightsErrors, ModelStats, InsightsUserInteractions, InsightsCache, CommandDetail
- Created `frontend/src/hooks/useInsights.ts` following the exact same pattern as `useSessionDetail`
- Hook connects to `/ws/insights/{sessionId}`, returns `{ insights, loading, error }`
- Auto-reconnects via `useWebSocketConnection`, cleans up on unmount
- Exported from `frontend/src/hooks/index.ts`
- Files changed: `frontend/src/types.ts`, `frontend/src/hooks/useInsights.ts`, `frontend/src/hooks/index.ts`, `ralph/prd.json`, `ralph/progress.txt`
- **Learnings for future iterations:**
  - Existing codebase uses a single `frontend/src/types.ts` file (not a `types/` directory) — keep all types there for import consistency (`'../types'`)
  - `useWebSocketConnection` handles reconnection with exponential backoff, cleanup on unmount — just reuse it, no custom WS logic needed
  - Hook pattern: `useState` for data/loading/error + `useCallback` for onMessage + `useWebSocketConnection` call — identical across all WS hooks
  - Backend sends `{ error: "..." }` on WS errors, so the hook's `onMessage` checks for `data.error` field
  - Hourly pattern keys from Python are integers (0-23), but JSON serializes them as string keys — TS types use `Record<string, ...>`
---
